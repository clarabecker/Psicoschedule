version: '3.8'

services:
  postgres:
    container_name: psicoschedule_postgres
    image: postgres:latest
    ports: 
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: psicoschedule
